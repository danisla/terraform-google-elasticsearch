# Copyright 2017 Google Inc.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

platform: linux
inputs:
- name: git
- name: git-pull-requests
  # optional: true # flag not available until concourse 3.9.x
outputs:
- name: email
image_resource:
  type: docker-image
  source:
    repository: google/cloud-sdk
    tag: alpine
params:
  git_src:
  git_target:
  service_account_json:
  project_id:
  backend_bucket:
  backend_prefix:
  env_name:
  TF_VAR_network_name:
  TF_VAR_cluster_name:
  TF_VAR_region:
  TF_VAR_zones:
run:
  path: bash
  args:
  - -exc
  - |
    set -o pipefail
    SA_JSON=${PWD}/service_account.json
    cat > ${SA_JSON} <<EOF
    $service_account_json
    EOF
    apk add --update jq curl
    gcloud auth activate-service-account --key-file=${SA_JSON}
    gcloud config set project $project_id
    gcloud config set compute/zone $zone

    BASE_DIR=${PWD}

    # Setup email
    EMAIL=${BASE_DIR}/email
    echo "terraform-google-elasticsearch/${env_name}: Failed to run example" > ${EMAIL}/subject-failed
    git --no-pager show > ${EMAIL}/body-failed

    EXAMPLE_DIR=examples/single-node

    TEST_DIR=${BASE_DIR}/${git_src}/${EXAMPLE_DIR}

    # Always destroy on exit.
    function cleanup {
      cd ${TEST_DIR}/ && terraform destroy -input=false -lock=false -auto-approve
    }
    trap cleanup EXIT

    cd ${TEST_DIR}

    # Install latest terraform
    curl -sL 	https://goo.gl/UYp3WG | bash
    source ${HOME}/.bashrc
    terraform version
    
    # Env for terraform google provider
    export GOOGLE_APPLICATION_CREDENTIALS=${SA_JSON}
    export GOOGLE_PROJECT=$project_id

    ### Begin Terraform example test

    # Remote backend
    cat > backend.tf <<EOF
    terraform {
      backend "gcs" {
        bucket     = "${backend_bucket}"
        prefix     = "${backend_prefix}"
      }
    }
    EOF
    
    # Run Terraform
    terraform init -upgrade=true
    terraform workspace select ${env_name} || terraform workspace new ${env_name}
    terraform plan
    terraform apply -input=false -lock=false -auto-approve | tee ${EMAIL}/body-failed

    ### Begin pull request test
    if [[ "${git_src}" != "${git_target}" ]]; then
      OLD_TEST_DIR="${TEST_DIR}"
      TEST_DIR="${BASE_DIR}/${git_target}/${EXAMPLE_DIR}"

      # Copy remote backend
      cp "${OLD_TEST_DIR}/backend.tf" "${TEST_DIR}/"

      cd ${TEST_DIR}

      # Run Terraform
      terraform init -upgrade=true
      terraform workspace select ${env_name} || terraform workspace new ${env_name}
      terraform plan
      terraform apply -input=false -lock=false -auto-approve | tee ${EMAIL}/body-failed
    fi